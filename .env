# 로컬 개발용 — Postgres 연결 문자열로 교체하세요.
# docker compose 로 로컬 Postgres를 함께 띄우면 아래 값으로 바로 동작합니다.
DATABASE_URL="postgresql://kolon:kolon@localhost:5432/kolon?schema=public"

# 세션 서명 시크릿 — 운영에서는 반드시 무작위 값으로 교체. (openssl rand -hex 32)
AUTH_SECRET="kolon-ops-dev-secret-change-in-production-0a1b2c3d4e5f6071"
